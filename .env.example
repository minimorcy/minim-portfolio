# GITHUB_TOKEN is required for fetching GitHub repository data.
# Create a personal access token at https://github.com/settings/tokens
# with at least `public_repo` scope (read-only).
GITHUB_TOKEN=your_github_pat_here
